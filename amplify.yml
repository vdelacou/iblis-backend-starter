version: 0.1
backend:
  phases:
    build:
      commands:
        - '# Execute Amplify CLI with the helper script'
        - chmod u+x ./amplifyPush.sh
        - ./amplifyPush.sh --simple
frontend:
  phases:
    build:
      commands: []
  artifacts:
    baseDirectory: /front
    files:
      - '**/*'
  cache:
    paths: []
